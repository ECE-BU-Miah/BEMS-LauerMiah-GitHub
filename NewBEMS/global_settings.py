import os

PROJECT_DIR = os.path.dirname(os.path.realpath(__file__))
WEBSERVER_DIR = PROJECT_DIR + '/WebServer/'
DRIVER_DIR = PROJECT_DIR + '/DeviceDrivers/'
AGENT_DIR = PROJECT_DIR + '/AgentPlatform/'
